# The rewrite version of discord.py, which is what I used while building MAT's Bot, isn't
# available on PyPi as of today, 7/2/2018. Instead, to get the rewrite version, use this
# command: "pip install -U git+https://github.com/Rapptz/discord.py@rewrite#egg=discord.py[voice]"
#
# Listed below are all of the required libraries used to make MAT's Bot. The ones that have been
# commented out are the dependencies installed with discord.py if you use the pip command above.
#
aiodns==1.1.1
# aiohttp==3.3.2
ascii==3.6
# async-timeout==3.0.0
# attrs==18.1.0
beautifulsoup4==4.6.0
cchardet==2.1.1  # Note: The library installed with discord.py is chardet. However, I replaced it
#                  with cchardet, which is the same as chardet, but much faster.
# cffi==1.11.5
decorator==3.4.0
# discord.py==1.0.0a1477+g184c430
# idna==2.7
# idna-ssl==1.0.1
lxml==4.2.3
# multidict==4.3.1
pycares==2.3.0
# pycparser==2.18
# PyNaCl==1.1.2
Pillow==5.2.0
# six==1.11.0
urllib3==1.23
validators==0.12.2
# websockets==5.0.1
# yarl==1.2.6